import type { Metadata } from 'next';
import Link from 'next/link';
import { GUIDES } from '@/lib/learn';
import { SITE_URL } from '@/lib/site';
export const metadata: Metadata = { title: 'Learn', description: 'Practical builder gel education for modern nail artists.', alternates: { canonical: `${SITE_URL}/learn` } };
export default function LearnPage() { return <main className="min-h-screen bg-[#f1eee7] text-[#121214]"><div className="mx-auto max-w-6xl px-6 py-8"><Link href="/" className="text-sm tracking-[.18em]">LUSTER STUDIO</Link><header className="max-w-3xl py-24"><p className="mb-5 text-xs uppercase tracking-[.25em] text-[#8d6b32]">The learning studio</p><h1 className="font-serif text-6xl leading-none md:text-8xl">Learn with intention.</h1><p className="mt-8 text-lg leading-8 text-black/65">Clear, practical education for artists building better services, stronger habits, and a more confident studio practice.</p></header><div className="grid gap-px border border-black/10 bg-black/10 md:grid-cols-2">{GUIDES.map((guide) => <Link key={guide.slug} href={`/learn/${guide.slug}`} className="bg-[#f1eee7] p-7 transition hover:bg-white"><div className="flex justify-between gap-4 text-xs uppercase tracking-[.16em] text-[#8d6b32]"><span>{guide.eyebrow}</span><span>{guide.readingTime}</span></div><h2 className="mt-12 font-serif text-3xl">{guide.title}</h2><p className="mt-4 leading-7 text-black/60">{guide.summary}</p><span className="mt-8 inline-block text-sm underline underline-offset-4">{guide.comingSoon ? 'Coming soon' : 'Read guide'} →</span></Link>)}</div></div></main> }
